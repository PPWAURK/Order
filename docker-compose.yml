services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: charene-menu
    restart: unless-stopped
    environment:
      DATABASE_URL: ${MENU_DATABASE_URL:-file:/app/data/menu.db}
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
